{
  "name": "@embody/testing",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "benchmark": "pnpm build && node benchmark.mjs",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": "^22.0.0 || ^24.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@embody/core": "workspace:*",
    "@embody/host": "workspace:*",
    "@embody/storage": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1"
  }
}
